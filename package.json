{
  "name": "ai-pdf-analysis",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "clean:uploads": "rm -rf src/data/uploads/*",
    "clean:cache": "rm -rf src/data/cache/*",
    "clean": "npm run clean:uploads && npm run clean:cache"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.830.0",
    "multer": "^2.0.1",
    "next": "15.3.4",
    "pdf2pic": "^3.2.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/multer": "^2.0.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "typescript": "^5"
  }
}
